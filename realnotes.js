document.getElementById(id).click();
window.location.assign(#notes/0)

parseInt(location.hash.split("#notes/")[1])
window.eventListener("hashchange", function)
use debugger!

Texting for the content to change in page!
# integration test (wait) or listen for the hashchange event

if your JS is running nothing else is running at the same time (tests for example)
